# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Spec generation backend flow (done)

## Current Goal

- None.

## Completed

- `01-design-system` — shadcn/ui configured, UI primitives installed (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), `lib/utils.ts` `cn()` helper, lucide-react, dark theme tokens in `globals.css`.
- `02-editor-chrome` — `EditorNavbar` (fixed-height top bar, sidebar toggle with PanelLeftOpen/PanelLeftClose), `ProjectSidebar` (floating overlay, slide-in from left, Projects header, My Projects / Shared tabs with empty states, New Project button), Dialog pattern updated with project color tokens (title, description, footer actions).
- `03-auth` — Clerk CLI initialized, `@clerk/nextjs` + `@clerk/ui` installed, proxy matcher with `/__clerk/(.*)`, `ClerkProvider` with shadcn theme in layout, sign-in/sign-up pages with two-panel layout (logo, tagline, feature list on left; Clerk form on right), auth-based redirects on `/`, UserButton in EditorNavbar.
- `04-project-dialogs` — Editor home screen with New Project button, Create/Rename/Delete project dialogs, sidebar project items with context menu actions (owned projects only), mobile backdrop scrim for sidebar, `useProjectDialogs` hook for dialog/form/loading state, `EditorProvider` context for page-level dialog access, mock project data with `slugify` helper.
- `05-prisma` — Prisma schema with `Project`/`ProjectCollaborator` models (indexes, cascade delete, unique constraints), cached Prisma client singleton in `lib/prisma.ts` with Accelerate/direct adapter branching, initial migration applied.
- `06-project-apis` — Backend REST API routes: `GET /api/projects` (list owner's projects), `POST /api/projects` (create with default name), `PATCH /api/projects/[projectId]` (rename, owner-only), `DELETE /api/projects/[projectId]` (delete, owner-only). Clerk auth enforced via `proxy.ts`, 401/403 responses handled. `npm run build` passes.
- `07-wire-editor-home` — Editor page is now a server component fetching owned + shared projects via `lib/projects.ts` helpers. `useProjectActions` hook manages dialog state and real API mutations (POST/PATCH/DELETE with fetch). `EditorShell` client component wires sidebar, dialogs, and navigation. Sidebar project items navigate to `/editor/[projectId]`. Create dialog shows room ID preview. `slugify` extracted to `lib/slug.ts` with `generateRoomId` helper. `npm run build` passes.
- `08-editor-workspace-shell` — `/editor/[roomId]` page with server-side access checks, `AccessDenied` for unauthorized users, workspace layout matching design screenshot: project name + subtitle in navbar, Share/AI buttons, UserButton, canvas placeholder with icon and descriptive text, AI sidebar with header/placeholder card/future hooks section, active project highlighted with cyan dot in sidebar. `roomId` field added to Prisma schema and Project type. `getProjectForUser` access helper in `lib/project-access.ts`. `handleCreate` now navigates using `roomId` from API response.
- `10-liveblocks-setup` — Liveblocks realtime collaboration infrastructure. `liveblocks.config.ts` with Presence (cursor with x/y, isThinking boolean) and UserMeta (id, info with name, avatar, color). Cached Liveblocks node client in `lib/liveblocks.ts` with `getOrCreateRoom` helper and deterministic color palette mapping. `POST /api/liveblocks-auth` route with Clerk auth, project access verification, room creation, and user metadata (name, avatar, cursor color). `@liveblocks/node` installed. `npm run build` passes.
- `11-base-canvas` — Liveblocks-backed React Flow canvas replacing the static placeholder. `types/canvas.ts` with `NodeData`, `EdgeData`, `CanvasNode`, `CanvasEdge` types, `NODE_COLORS` (8 fill/text pairs), `NODE_SHAPES` (6 shapes). `components/editor/liveblocks-canvas.tsx` client component with `LiveblocksProvider` (auth endpoint to `/api/liveblocks-auth`), `RoomProvider` (initial presence with cursor + isThinking), `ClientSideSuspense` with loading state, `LiveblocksErrorBoundary` with fallback. `useLiveblocksFlow` hook syncs nodes/edges with empty initial state. React Flow with `ConnectionMode.Loose`, `fitView`, `MiniMap`, dot-pattern `Background`. `globals.css` updated with `.react-flow` base background and minimap border styling. `npm run build` passes.
- `12-shape-panel` — Bottom shape panel for dragging shapes onto canvas. `types/canvas.ts` extended with `SHAPE_DEFAULT_SIZES` mapping (6 shapes with sensible dimensions). `components/editor/shape-panel.tsx` floating pill-shaped toolbar at bottom-center with draggable SVG icon buttons for rectangle, diamond, circle, pill, cylinder, hexagon. `components/editor/canvas-node.tsx` custom node renderer as bordered rectangle with centered label (default `NODE_COLORS[0]` fill). `liveblocks-canvas.tsx` updated with `ReactFlowProvider`, `nodeTypes` registration, `onDragOver`/`onDrop` handlers, `screenToFlowPosition` conversion, and `useMutation` to write new nodes to Liveblocks storage. Node IDs use `{shape}-{timestamp}-{counter}` pattern. New nodes use empty label, default color, and dragged shape. `npm run build` passes.
- `13-node-shape` — Proper shape rendering and drag preview. `canvas-node.tsx` updated: CSS shapes (rectangle, pill, circle) use subtle borders at rest (`rgba(255,255,255,0.1)`) that brighten on selection (`rgba(255,255,255,0.3)`). SVG shapes (diamond, hexagon, cylinder) use `vectorEffect="non-scaling-stroke"` for scalable borders with same selection treatment. `shape-drag-preview.tsx` new component renders a ghost preview (60% opacity, fixed positioning) that follows cursor during drag using default shape sizes. `shape-panel.tsx` accepts `onDragStart` callback prop to lift drag state to parent. `liveblocks-canvas.tsx` manages `draggingShape`/`dragPosition` state, listens for `dragover`/`dragend` events on document, renders `ShapeDragPreview` overlay, clears state on drop/end. `npm run build` passes.
- `14-node-editing` — Resize handles and inline label editing on canvas nodes. `types/canvas.ts` extended with `MIN_NODE_SIZES` (6 shapes with minimum dimensions). `components/editor/canvas-actions.tsx` new context providing `onNodeDataChange` callback for node data mutations. `canvas-node.tsx` rewritten: all shapes now fill their parent (`h-full w-full`) for proper resizing. Selected nodes show 8 corner/edge resize handles (subtle white border with directional cursors). Double-click opens inline label editing via absolutely-positioned textarea overlay centered on the node; empty labels show shape-appropriate placeholder text ("Event", "Store", "Label"). Editing closes on blur or Escape, keyboard events stop propagation to prevent canvas interactions. `liveblocks-canvas.tsx` updated with `CanvasActionsContext.Provider` wrapping React Flow, `handleNodesChange` wrapper enforcing minimum dimensions during resize via `MIN_NODE_SIZES`, and `handleNodeDataChange` callback writing label changes through `onNodesChange` replace operations. `npm run build` passes.
- `15-node-color-toolbar` — Floating color toolbar for changing node background and text colors. `types/canvas.ts` extended with `textColor` field on `NodeData`. `components/editor/node-color-toolbar.tsx` new component renders a floating pill-shaped toolbar above selected nodes, positioned using `useReactFlow().getNodes()` and viewport transform. Shows 8 color swatches from `NODE_COLORS`, active swatch highlighted with matching border and glow. Hover shows subtle shadow glow using text color. Click updates both `color` and `textColor` via `onNodeDataChange`. Toolbar uses `nodrag nopan` classes and `stopPropagation` to prevent canvas interactions. `canvas-node.tsx` updated: label elements now use `textColor` from node data instead of hardcoded `--text-primary`. `liveblocks-canvas.tsx` updated: imports `NodeColorToolbar`, computes `selectedNodeIds` from nodes array, renders toolbar inside canvas container. New node creation includes `textColor` default from `NODE_COLORS[0].text`. `npm run build` passes.
- `16-edge-behavior` — Custom canvas edges with connection handles, right-angle routing, hover/selection states, and inline label editing. `canvas-node.tsx` updated: removed per-shape Handle components, added all four connection handles (top, right, bottom, left) to the main node container with `id` props. Handles use `react-flow__handle` CSS class (white dots with dark border, hidden by default, fade in on node hover via CSS). Resize handles renamed to `RESIZE_HANDLE_STYLE` to avoid naming conflict. `globals.css` updated with `.react-flow__handle` styles (8px white circles, dark border, opacity 0 with 0.15s transition, scale on hover), `.react-flow__edge` cursor and selected state, and `.react-flow__connection-line` dashed accent stroke. Created `components/editor/canvas-edge.tsx` custom edge renderer using `getSmoothStepPath` with `borderRadius: 8` for clean right-angle routing. Renders invisible 20px-wide hit area path for easier hover/click, plus visual path with dimmed stroke at rest (`var(--text-muted)`, 1.5px) that brightens on hover/select (`var(--text-primary)`, 2px). `EdgeLabelRenderer` positions label at path midpoint: double-click opens growing input (saves on blur/Enter, cancels on Escape), saved labels render as pill badges, active unlabeled edges show faint "Add label" hint. All keyboard/pointer events stop propagation. `canvas-actions.tsx` extended with `onEdgeDataChange` callback. `liveblocks-canvas.tsx` imports `CanvasEdge`, registers `edgeTypes`, adds `defaultEdgeOptions` with `MarkerType.ArrowClosed` arrow marker and custom stroke. `handleEdgeDataChange` writes label changes through `onEdgesChange` replace operations. `npm run build` passes.
- `17-canvas-ergonomics` — Floating control bar and keyboard shortcuts for canvas navigation. `components/editor/canvas-control-bar.tsx` pill-shaped toolbar at bottom-left with zoom out/fit view/zoom in buttons (React Flow instance with 200ms animation) separated by a thin divider from undo/redo buttons (Liveblocks `useUndo`/`useRedo` hooks). `hooks/useKeyboardShortcuts.ts` listens for `+`/`=` (zoom in), `-` (zoom out), `Cmd/Ctrl+Z` (undo), `Cmd/Ctrl+Shift+Z` (redo), `Cmd/Ctrl+Y` (redo); ignores events while typing in inputs/textareas/contenteditable fields. `liveblocks-canvas.tsx` imports `useUndo`/`useRedo` from `@liveblocks/react`, uses `useKeyboardShortcuts` hook in `FlowCanvas`, renders `CanvasControlBar` inside canvas container. `npm run build` passes.
- `18-starter-templates` — Starter template library for importing pre-built diagrams. `components/editor/starter-templates.ts` defines `CanvasTemplate` type and `CANVAS_TEMPLATES` array with 3 templates: Microservices Architecture, CI/CD Pipeline, and Event-Driven System. Each template includes id, name, description, nodes with positions/shapes/colors, and edges. `components/editor/starter-templates-modal.tsx` dialog with scrollable grid of template cards, each showing a lightweight SVG diagram preview (nodes as colored rectangles, edges as lines with arrowhead markers, fitted to fixed viewport using bounds calculation), template name/description, and import button. `workspace-shell.tsx` adds Templates button (LayoutTemplate icon) to navbar, manages `templatesOpen` and `templateToImport` state, passes template to `LiveblocksCanvas`. `liveblocks-canvas.tsx` accepts `templateToImport`/`onTemplateImported` props; `FlowCanvas` effect removes all existing nodes/edges, adds template nodes/edges, calls `fitView({ duration: 300 })`, and clears pending template. `npm run build` passes.
- `19-presence-avatars-cursors` — Live presence indicators and cursors in the editor canvas. `liveblocks.config.ts` updated: `isThinking` renamed to `thinking` in Presence type. `components/editor/presence-avatars.tsx` renders an overlapping avatar group in the top-right corner of the canvas. Gets current user ID from Clerk `useAuth()`, filters Liveblocks `useOthers()` to exclude current user, renders up to 5 collaborator avatars (profile photo or initials fallback, colored background, subtle ring for dark canvas readability), +N overflow chip when more than 5, thin divider between collaborator avatars and Clerk `UserButton` only when collaborators exist. `components/editor/live-cursors.tsx` renders colored SVG cursor pointers with name badges for other participants using `useOthers()`. `liveblocks-canvas.tsx` updated: imports `useUpdateMyPresence`, adds `onMouseMove` handler broadcasting cursor position `{x, y}` to presence, `onMouseLeave` clearing cursor to null, renders `LiveCursors` and `PresenceAvatars` (positioned top-right with `z-40`). `workspace-shell.tsx` adds `useAuth()` import, passes `userId` as `currentUserId` prop to `LiveblocksCanvas`. `npm run build` passes.
- `20-ai-sidebar-shell` — Floating AI sidebar extracted to `components/editor/ai-sidebar.tsx`. Open/close state controlled by `WorkspaceShell` via `isOpen`/`onClose` props; slide-in animation from the right preserved (`translate-x`, opacity, pointer-events). Header with Bot icon, "AI Workspace" title, "Collaborate with Ghost AI" subtitle, and close button. shadcn `Tabs` with AI Architect and Specs panels. AI Architect tab: scrollable chat area, empty state with bot icon and three starter prompt chips (`bg-subtle`, `text-ai-text`), user message bubbles (`bg-accent-dim border-brand/50 border-2`), local send-only input with auto-resizing textarea (72px–160px), Enter/Shift+Enter handling, and `bg-brand text-white` send button. Specs tab: `Generate Spec` button and static demo spec card (`bg-elevated`, file icon, snippet preview, disabled download). Inline placeholder sidebar removed from `workspace-shell.tsx`. `npm run build` passes.
- `21-canvas-autosave` — Canvas persistence via Vercel Blob and Prisma `canvasJsonPath`. `@vercel/blob` installed. `PUT`/`GET /api/projects/[projectId]/canvas` routes upload canvas JSON to `canvas/{projectId}.json`, store blob URL on the project record, and return saved state (owner/collaborator access via `getProjectByIdForUser`). `hooks/use-canvas-autosave.ts` debounces saves (1.5s), tracks `idle`/`saving`/`saved`/`error` status, loads saved canvas when the Liveblocks room is empty, and skips load when nodes or edges already exist. `LiveblocksCanvas` wires the hook; `WorkspaceShell` navbar Save button shows status labels and triggers manual save. `npm run build` passes.
- `22-trigger-setup` — Trigger.dev v4.4.6 integrated. `@trigger.dev/sdk` and `@trigger.dev/build` installed. `trigger.config.ts` configured with Prisma 7 engine-only build extension. `trigger/example.ts` sample task added. `trigger:dev` and `trigger:deploy` npm scripts added. Requires `TRIGGER_PROJECT_REF` and `TRIGGER_SECRET_KEY` env vars.
- `22-design-agent-api` — Design generation backend wiring (no AI logic). `TaskRun` Prisma model (`runId` PK, `projectId`, `userId`, `createdAt`, compound index on `userId` + `projectId`). `POST /api/ai/design` validates prompt/roomId/projectId, checks project access, triggers `design-agent` task, persists `TaskRun`, returns `runId`. `POST /api/ai/design/token` verifies `TaskRun` ownership and returns a run-scoped Trigger.dev public token. `trigger/design-agent.ts` minimal task logs/echoes `prompt` and `roomId`. `npm run build` passes.
- `23-design-agent-logic` — Full AI design agent task. `types/tasks.ts` with `aiStatusFeedMessageSchema` for `ai-status-feed` payloads. `lib/liveblocks-collaborative-flow.ts` wraps `mutateFlow`, ephemeral AI presence (`setPresence`), and status feed publishing (`createFeedMessage`). `lib/design-agent/` Gemini plan generation (`generateObject` + `@ai-sdk/google`) and canvas action application (add/move/resize/update/delete nodes, add/delete edges) with palette/shape/layout validation. `trigger/design-agent.ts` orchestrates status messages, thinking presence, progressive canvas updates, and error cleanup. `npm run build` passes.
- `24-ai-presence-state` — Shared AI activity UI. `lib/liveblocks-constants.ts` with `AI_STATUS_FEED_ID` and `AI_AGENT_USER_ID` for safe client imports. `hooks/use-ai-generation-state.ts` subscribes to `ai-status-feed` via `useFeedMessages`, validates payloads with `parseAiStatusFeedMessage`, and derives `isGenerating` from room presence `thinking`. `ai-sidebar.tsx` shows a shared status bar, disables chat input/send while generating, and shows a spinner on the send button. `live-cursors.tsx` shows a spinner in name badges when `thinking: true`. `AiSidebar` moved inside `RoomProvider` in `liveblocks-canvas.tsx`. `liveblocks.config.ts` typed `FeedMessageData`. `npm run build` passes.
- `25-sidebar-chat-feed` — Room-scoped Liveblocks `ai-chat` feed for collaborative sidebar chat. `lib/liveblocks-constants.ts` adds `AI_CHAT_FEED_ID` separate from `AI_STATUS_FEED_ID`. `types/tasks.ts` adds `aiChatFeedMessageSchema` (sender, role, content, timestamp) and `parseAiChatFeedMessage`. `hooks/use-ai-chat-feed.ts` ensures the feed exists, subscribes via `useFeedMessages`, and sends with `useCreateFeedMessage`. `ai-sidebar.tsx` renders validated messages with sender and timestamp, clears input on successful send, and shows a compact send error. `liveblocks.config.ts` extends `FeedMessageData` for chat payloads. `npm run build` passes.
- `26-ai-chat-functional` — AI sidebar submit wired to design generation. On send: user message to `ai-chat`, `POST /api/ai/design` with `{ prompt, roomId, projectId }`, run-scoped token via `/api/ai/design/token` when needed, local `runId`/`publicToken` state, and `useRealtimeRun` for status. Input/send disabled while a run is active; completion pushes an assistant message to `ai-chat`. Compact status strip above input reads `ai-status-feed` during active runs. Canvas updates remain Liveblocks-driven. `hooks/use-ai-chat-feed.ts` adds `sendAssistantMessage`. `AiSidebar` receives `roomId`/`projectId` from `LiveblocksCanvas`. `npm run build` passes.
- `27-spec-generation-flow` — Spec generation backend wiring (no frontend). `lib/spec-agent/schemas.ts` Zod schemas for trigger request and task payload (`roomId`, `chatHistory`, `nodes`, `edges`; project access resolved server-side). `POST /api/ai/spec` authenticates user, resolves project via `getProjectForUser(roomId)` (no client `projectId`), triggers `generate-spec` task, persists `TaskRun`, returns `runId`. `POST /api/ai/spec/token` verifies `TaskRun` ownership and returns 1h run-scoped public token. `trigger/generate-spec.ts` `schemaTask` validates payload, calls Gemini via `generateText`, updates run metadata (`status`, `progress`), returns `{ spec }` Markdown output. `lib/spec-agent/prompt.ts` and `generate-spec-content.ts` build prompts and generate content. `npm run build` passes.

## In Progress

- None.

## Next Up

- `28-spec-persistence-download` — Persist generated specs to Vercel Blob and add secure download route.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- `roomId` is a unique, human-readable slug (e.g., `my-project-a1b2c3`) separate from the database `id` (cuid). The URL uses `roomId`; the database primary key uses `id`.

## Session Notes

- Editor chrome components live in `components/editor/`. Sidebar state is managed by parent components (wired in later specs). Dialog styling uses `globals.css` tokens — ready for project dialogs in `04-project-dialogs`.
- Project dialogs implemented with dedicated components per dialog type. `useProjectDialogs` hook manages all dialog/form state. `EditorProvider` context passes `onNewProject` to the editor page without prop drilling through the layout. Sidebar actions use a simple popover menu pattern (not a full dropdown component). Mock data lives in `lib/mock-projects.ts`.
- Prisma schema uses multi-file structure: `prisma/schema.prisma` (generator + datasource) + `prisma/models/project.prisma` (models). Generated client outputs to `app/generated/prisma/`. Import via `@/app/generated/prisma/client`. Prisma 7's `prisma-client` generator produces `.ts` files — import path must be explicit (no barrel index). Client singleton in `lib/prisma.ts` caches on `globalThis` for dev hot reloads. Accelerate path requires `@prisma/extension-accelerate` (not yet installed).
- Next.js 16 uses `proxy.ts` instead of `middleware.ts` for request interception. Clerk middleware is configured in `proxy.ts` at project root. Route handlers use `auth()` from `@clerk/nextjs/server` for per-request auth checks. Dynamic route params are `Promise<{ projectId: string }>` in Next.js 16.
- `07-wire-editor-home` refactored the editor page to a server component that fetches real project data via `getOwnedProjects()` and `getSharedProjects()` from `lib/projects.ts`. The layout became minimal (just renders children), with all chrome moved to `EditorShell` client component. `useProjectActions` hook replaces `useProjectDialogs` for mutation handling — it manages dialog state, calls real API endpoints, handles navigation after create, and refresh/redirect after delete. `slugify` moved to `lib/slug.ts` with `generateRoomId` for room ID generation. Sidebar now accepts `onSelect` callback for project navigation.
- `08-editor-workspace-shell` added `roomId` field to the Prisma schema (unique, required), `Project` type, and all data helpers. `lib/project-access.ts` provides `getCurrentUser()` and `getProjectForUser(roomId)` access helpers. `components/editor/access-denied.tsx` renders centered lock icon + message + back link. The `[roomId]/page.tsx` server component handles auth redirect, project lookup by `roomId`, and renders either `AccessDenied` or `WorkspaceShell`. `WorkspaceShell` client component contains the workspace navbar (project name + "Workspace" subtitle, Share button with Link2 icon, AI button with Sparkles icon, UserButton), the existing `ProjectSidebar` overlay with `activeProjectId` highlight (cyan dot + accent background), a canvas placeholder area (compass icon, "WORKSPACE SHELL" heading, descriptive text, teal gradient background), and a togglable AI sidebar (AI Copilot header, placeholder card with chat surface pending message, future hooks section). `handleCreate` in `use-project-actions` now uses the project's `roomId` from the API response for navigation.
- `09-share-dialog` added `lib/clerk.ts` with `createClerkClient` singleton and `enrichCollaboratorEmails` helper that looks up Clerk users by email to get display name and avatar. Fixed `lib/project-access.ts` to check collaborator access using `sessionClaims.email_address` instead of comparing email to userId. Added API routes under `/api/projects/[projectId]/collaborators/`: `GET` lists collaborators with enriched user data (owner or collaborator access), `POST` invites by email (owner-only, validates email format, prevents self-invite, checks duplicates), `DELETE` removes a collaborator (owner-only). `components/editor/share-dialog.tsx` is a client dialog with email input, collaborator list (avatar or initial, name, email), remove buttons for owners, and copy link button with "Copied!" feedback. `WorkspaceShell` wired the existing Share button to toggle the dialog.
- `10-liveblocks-setup` added `liveblocks.config.ts` with global `Liveblocks` type declarations for Presence (cursor with x/y, isThinking boolean) and UserMeta (id, info with name, avatar, color). `lib/liveblocks.ts` contains a cached `Liveblocks` node client singleton and a `getOrCreateRoom` helper that ensures the room exists before returning. `getColorForUser` deterministically maps a user ID to one of 8 fixed colors (cyan, indigo-purple, orange, red, pink, green, teal, amber). `POST /api/liveblocks-auth` route requires Clerk auth, verifies project access via `getProjectForUser`, calls `getOrCreateRoom` with the project's `roomId`, then returns a Liveblocks ID token with user metadata (name, avatar, cursor color). `@liveblocks/node` installed as dependency.
- `11-base-canvas` created `types/canvas.ts` with `NodeData` (label, color, shape extending Record<string, unknown>), `EdgeData` (optional label), `CanvasNode`, `CanvasEdge` type aliases, `NODE_COLORS` array of 8 fill/text color pairs, and `NODE_SHAPES` array of 6 shape strings. Created `components/editor/liveblocks-canvas.tsx` as a "use client" component that wraps the canvas in `LiveblocksProvider` (authEndpoint callback POSTing to `/api/liveblocks-auth`), `RoomProvider` (initialPresence with cursor null + isThinking false), `ClientSideSuspense` with loading state, and a `LiveblocksErrorBoundary` with connection error fallback. Inner `Flow` component uses `useLiveblocksFlow({ suspense: true })` to get synced nodes/edges/handlers, renders `ReactFlow` with `ConnectionMode.Loose`, `fitView`, `MiniMap`, and dot-pattern `Background` (color from `--border-subtle`, gap 20, size 1). `workspace-shell.tsx` replaced `CanvasPlaceholder` with `LiveblocksCanvas` passing `project.roomId`. Added `.react-flow` background and minimap border CSS to `globals.css`. `ConnectionMode` enum import required over string literal due to TypeScript constraints. `NodeData`/`EdgeData` needed `extends Record<string, unknown>` to satisfy `Node`/`Edge` generic constraints.
- `13-node-shape` improved node shape rendering and added drag preview. `canvas-node.tsx` shapes now use selection-aware borders: CSS shapes (rectangle, pill, circle) use `rgba(255,255,255,0.1)` at rest and `rgba(255,255,255,0.3)` when selected via inline `style` objects; SVG shapes (diamond, hexagon, cylinder) use `stroke` with `vectorEffect="non-scaling-stroke"` for scalable borders. Created `shape-drag-preview.tsx` with six preview shape components (PreviewRectangle, PreviewDiamond, PreviewCircle, PreviewPill, PreviewCylinder, PreviewHexagon) that render at default sizes with 60% opacity and fixed positioning. `shape-panel.tsx` now accepts `onDragStart` callback prop to notify parent of drag initiation. `liveblocks-canvas.tsx` FlowCanvas manages `draggingShape` (string | null) and `dragPosition` ({x, y}) state; `useEffect` listens for `dragover`/`dragend` on `document` to track position and clear state; passes `handleShapeDragStart` to `ShapePanel`; renders `ShapeDragPreview` conditionally when `draggingShape` is set.
- `14-node-editing` added resize handles and inline label editing to canvas nodes. Created `canvas-actions.tsx` with `CanvasActionsContext` providing `onNodeDataChange(id, data)` for node data mutations. `canvas-node.tsx` rewritten: shapes now use `h-full w-full` to fill their parent for proper resize behavior. Selected nodes display 8 resize handles at corners and edges (tiny rounded divs with `border-white/20`, directional cursors like `cursor-nw-resize`). Double-click on the label area opens an absolutely-positioned textarea centered over the node (`left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2`); empty labels show shape-specific placeholder text. Editing exits on blur (saves) or Escape (cancels). All keyboard events stop propagation to prevent canvas pan/drag during typing. `liveblocks-canvas.tsx` wraps React Flow in `CanvasActionsContext.Provider`. `handleNodesChange` intercepts dimension changes during resize to enforce `MIN_NODE_SIZES` per shape type. `handleNodeDataChange` writes label changes through `onNodesChange` with `replace` type operations. `types/canvas.ts` extended with `MIN_NODE_SIZES` mapping (rectangle 100x50, diamond 80x80, circle 60x60, pill 100x50, cylinder 80x100, hexagon 100x80).
- `15-node-color-toolbar` added a floating color toolbar for changing node fill and text colors. `types/canvas.ts` extended `NodeData` with `textColor: string` field. Created `components/editor/node-color-toolbar.tsx` — a pill-shaped floating toolbar positioned above selected nodes using `useReactFlow().getNodes()` and viewport transform calculation. Renders 8 color swatches from `NODE_COLORS` with active swatch highlighted (border + glow matching text color). Hover adds subtle shadow glow using the swatch's text color. Click on a swatch calls `onNodeDataChange` for each selected node with both `color` (fill) and `textColor` (paired text color). Uses `nodrag nopan` classes and `stopPropagation` on all pointer events to prevent canvas drag/pan interactions. `canvas-node.tsx` updated label rendering to use `textColor` from node data via inline `style` instead of hardcoded `--text-primary`. `liveblocks-canvas.tsx` computes `selectedNodeIds` from nodes array and renders `NodeColorToolbar` inside the canvas container. Drop handler now includes `textColor: NODE_COLORS[0].text` as default. All changes sync through Liveblocks collaborative state — no server calls.
- `17-canvas-ergonomics` added a floating control bar for zoom and undo/redo, plus keyboard shortcuts. Created `components/editor/canvas-control-bar.tsx` — pill-shaped toolbar at bottom-left (above shape panel) with two groups separated by a thin divider: zoom controls (ZoomOut, Maximize2, ZoomIn from lucide-react) wired to React Flow instance with `{ duration: 200 }` animation, and history controls (Undo2, Redo2) wired to Liveblocks `useUndo`/`useRedo` hooks. Created `hooks/useKeyboardShortcuts.ts` — receives `ReactFlowInstance`, `undo`, and `redo` handlers; listens on `window` keydown; skips shortcuts when target is an input/textarea/select or contenteditable element. Supports: `+`/`=` for zoom in, `-` for zoom out, `Cmd/Ctrl+Z` for undo, `Cmd/Ctrl+Shift+Z` for redo, `Cmd/Ctrl+Y` for redo. `liveblocks-canvas.tsx` refactored: `FlowCanvas` now uses full `useReactFlow()` instance (replacing destructured `screenToFlowPosition`), adds `useUndo`/`useRedo` from `@liveblocks/react`, invokes `useKeyboardShortcuts`, and renders `CanvasControlBar` inside the canvas container. `handleDrop` updated to use `reactFlow.screenToFlowPosition`.
- `18-starter-templates` added a prebuilt diagram library. Created `components/editor/starter-templates.ts` with `CanvasTemplate` type and `CANVAS_TEMPLATES` array containing 3 templates: Microservices (API gateway, auth, user/order services, event bus, dedicated databases), CI/CD Pipeline (repo, CI trigger, build, parallel test/lint/security, approval gate, staged deployment, monitoring), and Event-Driven System (HTTP API, auth, order/payment services, event bus, notification/analytics/inventory workers, databases). Each template node uses `node()` helper that maps shape to default sizes and color index to `NODE_COLORS`. Created `components/editor/starter-templates-modal.tsx` with `DiagramPreview` SVG component: computes node bounds, calculates scale/offset to fit in 320x220 viewport, renders nodes as rounded rectangles with their fill color, edges as lines with arrowhead markers, and a subtle border. Modal uses `Dialog` with scrollable 2-column grid of template cards. `workspace-shell.tsx` added Templates button (LayoutTemplate icon) in navbar between Share and AI buttons, manages `templatesOpen` state and `templateToImport` state, passes to `LiveblocksCanvas`. `liveblocks-canvas.tsx` extended with `templateToImport`/`onTemplateImported` props threaded through `LiveblocksCanvas` → `Flow` → `FlowCanvas`. Import effect: removes all existing node/edge IDs via remove changes, adds template nodes/edges via add changes, calls `reactFlow.fitView({ duration: 300 })` after a `requestAnimationFrame` to ensure React Flow has processed the additions, then clears the pending template. `npm run build` passes.
